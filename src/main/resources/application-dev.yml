spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_dev?useUnicode=true&serverTimezone=UTC&useSSL=true&verifyServerCertificate=false
    username: sfg_dev_user
    password: guru
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true